{
  "name": "electron-notes",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . electron-notes --overwrite --asar --platform=darwin --arch=x64 --prune=true --out=../release-builds",
    "package-linux": "electron-packager . electron-notes --overwrite --asar --platform=linux --arch=x64 --icon=pics/notes_icon.png  --prune=true --out=../release-builds"
  },
  "author": "Rick Lan",
  "license": "ISC",
  "devDependencies": {
    "electron": "^16.0.0",
    "electron-packager": "^15.4.0"
  }
}
